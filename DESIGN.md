# 設計メモ

## 目的

このプロジェクトは、型検査を通過するTypeScriptコードでも、配列の破壊的変更によって呼び出し元の状態を壊し得ることを、小さな振る舞いテストで示します。対象は`Array.prototype.sort()`による配列コンテナの順序変更だけです。

## 実行基盤の選択

Node.js 22以降を前提とし、TypeScriptコンパイラとNode.js標準テストランナーを使います。JestやVitestを導入せず、テスト失敗が依存設定ではなくアサーション差分として読める構成にしました。ESMでコンパイル後のテストを実行するため、テストからの相対importには`.js`拡張子を使います。

`strict`、`noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`を有効にしています。バグ状態でもこれらの型検査は成功します。これは型設定の不足を示すものではなく、今回守るべき「入力配列を変更しない」という契約が、関数の戻り値型だけでは表されていないことを示す観測です。

## テスト境界

`rankTasksByPriority`は外部I/Oを持たない決定的な関数です。そのため、最小の単体テストで、返り値、呼び出し後の入力配列、参照の同一性を観測します。実装詳細であるスプレッド構文の利用はテストしません。

## 不変性の範囲

`[...tasks]`は配列コンテナを浅く複製します。したがって、順序を保つ契約は満たしますが、個々の`Task`オブジェクトへの変更を防ぐものではありません。要素オブジェクトも不変に扱う必要がある場合は、更新用関数を別に設計し、対象プロパティと期待する最終状態を追加テストで固定します。

## 参考教材との関係

段階的に失敗を観測してから最小修正を行う学習方法を採用していますが、既存教材の文章、章名、コードは複製していません。
